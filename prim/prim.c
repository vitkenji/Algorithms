#include "prim.h"

void prim(MGraph* G, int size)
{
    int* keys = (int*) malloc(sizeof(int));
    int* predecessors = (int*) malloc(sizeof(int));
    bool* visited = (bool*) malloc(sizeof(bool)*size);

}